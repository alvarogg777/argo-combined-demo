apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-dex-server
spec:
  template:
    spec:
      imagePullSecrets:
      - name: regcred
      initContainers:
      - name: copyutil
        # image: alinbalutoiu/argocd:v2.0.0
        image: cyrilix/argocd:v2.0.1
