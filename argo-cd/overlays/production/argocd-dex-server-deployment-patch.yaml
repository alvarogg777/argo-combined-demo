apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-dex-server
spec:
  template:
    spec:
      imagePullSecrets:
      - name: regcred
      initContainers:
      - name: copyutil
        image: jas612/argocd:v2.1.0
        env:
          - name: TZ
            value: Europe/Madrid
