apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-dex-server
spec:
  template:
    spec:
      imagePullSecrets:
      - name: regcred
      initContainers:
      - name: copyutil
        image: truebrain/argocd:v2.1.1
        env:
          - name: TZ
            value: Europe/Madrid
