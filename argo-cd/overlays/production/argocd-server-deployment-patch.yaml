apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-server
spec:
  template:
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: argocd-server
        image: alvarogg777/argocd:v2.1.6
        command: [argocd-server, --staticassets, /shared/app, --insecure]
        env:
          - name: TZ
            value: Europe/Madrid
