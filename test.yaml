apiVersion: v1
kind: Pod
metadata:
  name: cuda
  namespace: test
spec:
  containers:
  - name: nvidia-base
    image: alvarogg777/arm64v8:nvidia-l4t-base-32.5.1
    resources:
      limits:
        nvidia.com/gpu: 1 # requesting 2 GPUs
  imagePullSecrets:
  - name: regcred

